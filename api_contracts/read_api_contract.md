# API Contract: Raw CSV Ingestion

This document defines the API contract for retrieving status and data associated with uploaded canonical lab analyzer CSVs. The data can be retrieved by ingestion_id and by patient_id (non-PIH identifier). 

---

## `POST /v1/ingestions`

### Summary
