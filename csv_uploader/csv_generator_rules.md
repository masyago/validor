# CSV generator rules

CSV generator produces canonical clinical analyzer output.
The generator is provided with a set panels and tests it can use for the 
CSV.

## CSV columns: 

run_id: required
sample_id: optional
patient_id: required
panel_code: required
test_code: required
test_name: optional
analyte_type: optional
result: required
units: optional
reference_range_low: optional
reference_range_high: optional
flag: optional
collection_timestamp: required
instrument_id: required


`run_id` generated by the script (already included)
`sample_id` generated by the script. mostly one per CSV. 
`patient_id` one per CSV
`result` selected by the generator. Mostly between reference_range_low and reference_range_high with values outside the range
`flag` optional, generated based on `result` value: low, high, or questionable result (needs manual review/dilution).
`collection_timestamp` script generated (datetime.now()). One timestamp per panel (not per individual test)
`instrument_id` retrieve from config.py file


Select one row from defined list to select combo of:
* panel
* panel_name
* test_code
* test_name
* analyte_type
* units
* reference_range_low
* reference_range_high

Example of the row in a defined list (18 rows total):
Number	Blood chemistry test	Analyte abbreviation (test_code)	Analyte name(test_name)	Analyte type	Units	Low range	High range
1	Basic Metabolic Panel (BMP)	Na	Sodium	electrolyte	mEq/L	135	145
2	Basic Metabolic Panel (BMP)	K	Potassium	electrolyte	mEq/L	3.7	5.2


